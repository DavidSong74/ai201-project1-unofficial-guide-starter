sentence-transformers==3.4.1
chromadb>=0.6.0
groq==0.15.0
python-dotenv==1.0.1

# Query interface (Milestone 5) — FastAPI backend (api.py) for the web/ React UI:
fastapi>=0.115.0
uvicorn>=0.30.0

# Document ingestion — add if your documents/ folder contains .pdf files:
# pdfplumber==0.11.4
